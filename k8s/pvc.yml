apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mysql-pvc
  namespace: nodejs

spec:
  volumeName: mysql-pv   
   
  storageClassName: "" 
  
  accessModes:
    - ReadWriteOnce

  resources:
    requests:
      storage: 1Gi